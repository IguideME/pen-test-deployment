apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: "{{ .Release.Namespace }}"
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: {{ .Values.image.frontend.port }}